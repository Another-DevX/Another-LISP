WHITESPACE = _{ " " | "\t" | "\n" }
number = @{ "-"? ~ ASCII_DIGIT+ }
operator = { "+" | "-" | "*" | "/" }
expression = { "(" ~ operator ~ expression+ ~ ")" | number }
anotlisp = { operator ~ expression+ }
